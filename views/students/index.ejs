<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>All Students</h1>
    <ul>
        <% students.forEach(function(student) { %>
            <!-- <li>Feed Dogs - done</li> -->
            <li><%= student.student %> - <%= student.present ? 'PRESENT' : 'ABSENT' %></li>
        <% }); %>
         %>
    </ul>
</body>
</html>